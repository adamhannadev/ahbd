
<div class="ui centered <% if @package.active %> green <% else %> red <% end %> card" style="width:600px;">
  <div class="content">
    <div class="header"><h1>Package ID - <%= @package.id %></h1></div>
    <div class="meta">
      <span class="category"><h3><%= @package.title %></h3></span>
    </div>
    <div class="description">
      <div class="ui three statistics">
  <div class="statistic">
    <div class="value">
      <%= @package.lessons_purchased %>
    </div>
    <div class="label">
      Purchased
    </div>
  </div>
  <div class="statistic">
    <div class="value">
      <%= @package.lessons.count %>
    </div>
    <div class="label">
      Used
    </div>
  </div>
  <div class="statistic">
    <div class="value">
      <%= @package.lessons_purchased - @package.lessons.count %>
    </div>
    <div class="label">
      Remaining
    </div>
  </div>
</div>
    </div>
  </div>
  <div class="extra content">
    <div class="right floated author">
      <% if @package.active %>Active <% else %> Inactive <% end %>
    </div>
  </div>
</div>
<div class="ui relaxed divided list">
  <% @package.lessons.each do  |l| %>
  <div class="item">
    <i class="large calendar middle aligned icon"></i>
    <div class="content">
    <%= link_to l.lesson_date.strftime("%m/%d/%Y at %I:%M%p"), lessons_path %></a>
      <div class="description">Lesson ID: <%= l.id %></div>
    </div>
  </div>
  <% end %>
</div>

<%= link_to 'Edit', edit_package_path(@package) %> |
<%= link_to 'Back', packages_path %>

