    <div class="card">
        <h3 class="card-title row">
           <div class="col col-sm-4">
           Package ID - <%= @package.id %>
           </div>
           <div class="col col-sm-4 col-sm-offset-4">
               <div class="float-right"><%= @package.title %></div>
           </div>
        </h3>
    <div class="row">
        <div class="col col-sm-2">
            
        </div>
        <div class="col col-sm-8">
            <div class="container-small row align-center">
              <h3 class="col col-sm-4">Purchased: <%= @package.lessons_purchased %></h3>
              <h3 class="col col-sm-4">Used: <%= @package.lessons.count %></h3>
              <h3 class="col col-sm-4">Remaining: <%= @package.remaining %></h3>
        </div>
        </div>
        <div class="col col-sm-2">
            
        </div>
    </div>    
        
       <ul class="card-actions">
          
        </ul>
        <hr/>
      </div>
      <div class="row">
  <% @package.lessons.each do  |l| %>
    
    <div class="card col col-sm-3">
          <%= link_to l.lesson_date.strftime("%m/%d/%Y at %I:%M%p"), lessons_path %></a>
      <div class="description"><%= l.student.last_name %> Lesson ID: <%= l.id %></div>
    </div>

  <% end %>
</div>

<%= link_to 'Edit', edit_package_path(@package) %> |
<%= link_to 'Back', packages_path %>

