<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
    <div class="field">
       <%= form.label :bookable_type %>
        <%= form.text_field :bookable_type %>
    </div>
    
    <div class="field">
      <%= form.label :bookable_id, "Bookable ID" %>
      <%= form.text_field :bookable_id %>
    </div>

<div class="field">
    <%= form.label :recurring %>
    <%= form.select_recurring :recurring, nil, :allow_blank => true %>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
